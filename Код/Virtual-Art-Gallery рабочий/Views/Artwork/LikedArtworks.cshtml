@model IEnumerable<Virtual_Art_Gallery.Models.ArtworkModel>

<h2>Понравившиеся публикации</h2>

<div style="display: flex; flex-wrap: wrap; gap: 20px;">
    @foreach (var artwork in Model)
    {
        <div style="border: 1px solid #ccc; padding: 15px; border-radius: 10px; width: 300px; text-align: center;">
            <img src="@artwork.ImagePath" alt="@artwork.Title" style="width: 100%; height: auto; max-height: 200px;" />                 
            <h3>@artwork.Title</h3>
            <p>@artwork.Description</p>
            <p><strong>Категория:</strong> @artwork.Category.Name</p>
            <p><strong>Лайки:</strong> @artwork.LikeCount</p>

            <a href="@Url.Action("Details", "Artwork", new { id = artwork.Id })" class="btn btn-primary">Посмотреть</a>
        </div>
    }
</div>
